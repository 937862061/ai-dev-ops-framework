---
name: ai-dev-ops-framework
description: 在新项目或存量项目建立可审计的 AI 开发与运维协作体系时使用。完成必需的 P0-P2 基线、首次 AI 开发导览和项目知识入口；真实需求试点与治理推广按需执行。
---

# AI 开发运维体系建设

本 skill 用于把项目事实沉淀为一套可由不同 AI 工具复用的协作体系。目标是让 AI 在开发、排查、数据修复与运维前，先知道项目知识从哪里来、何时必须向开发者确认，以及哪些操作必须遵守项目级规范。

不把来源项目的业务术语、技术栈、数据库规则或环境信息直接复制到目标项目。它们必须在 P1/P2 中依据目标项目的代码、文档、配置和开发者确认重新提炼。

## 交付边界

- 目标项目中的 `.agents/skills/` 是项目级 skill 的唯一维护源，应随项目版本管理；不同 AI 工具可同步、链接或直接读取该目录，但不得把项目规则复制到用户全局 skill 目录。
- skill 只作为轻量入口、路由和约束；易变的项目事实放在项目知识库，按需读取，避免把整个项目塞进上下文。
- 业务概念、字段、枚举、状态流转、真实执行链路和线上操作都必须以目标项目事实为准。文档缺失、冲突或有歧义时，明确提问，不以通用经验补全。
- 初始化体系不是授权修改业务代码、生产数据或外部系统；此类操作仍须按目标项目规则和开发者授权执行。

## 必须完成：P0-P2

按 [P0-P2 落地指南](references/p0-p2.md) 执行。P0 先建立范围、权威来源和维护责任；P1 再从目标项目提炼知识资产；P2 将其接入项目级 skill 和跨工具入口。每个阶段都必须满足对应验收条件，不能以空泛目录或通用模板替代项目事实。

首次新建时，可使用 [项目模板](references/project-templates.md) 创建最小骨架；随后必须以目标项目实际情况替换所有尖括号占位符。模板不是事实来源。

## 每项目强制的首次 AI 开发导览

每个项目都必须提供“首次 AI 开发导览”，并由根 `AGENTS.md` 与项目 knowledge skill 共同强制。完整触发条件、允许动作、状态隔离和异常处理见 [首次导览规则](references/first-onboarding.md)。

关键约束：导览完成状态只属于“当前开发者 + 当前 AI 工具 + 当前项目标识”，项目标识优先使用 Git 远程地址，没有远程地址才使用绝对路径。状态只能保存在该工具的本地持久记忆，绝不能写入仓库、Git 配置、数据库或其他共享介质。

为避免自相矛盾，首次搭建本体系时仅可先创建 P0 所需的根入口与导览文档；在开发者确认导览前，不得借“搭建体系”之名检索目标项目代码、配置、表结构、日志或当前需求。确认后才继续 P1/P2 的事实提炼。

## 可选：P3-P4

P3（真实需求试点）和 P4（固化推广/定期治理）不是基础交付前置条件，除非开发者明确要求或项目风险需要。需要时按 [P0-P2 落地指南](references/p0-p2.md) 的 P3/P4 节执行；不要把试点记录、季度巡检或组织推广伪装成已完成的 P0-P2。

## 完成时说明

报告：已建立的项目入口、三类项目 skill、知识资产及首次导览；P0-P2 每项验收证据；仍待项目事实填充的部分；以及 P3/P4 是否未执行。对代码、接口、数据库、线上环境和第三方操作，明确区分静态核对与未执行验证。
